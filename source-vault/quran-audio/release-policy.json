{
  "schema": 1,
  "state": "pending_vendor_import",
  "required_manifest_sha256": null,
  "required_pack_id": null,
  "on_missing": "ALLOW_OFFLINE_WITHOUT_PRONUNCIATION_UNTIL_VENDOR_IMPORT_COMPLETES",
  "note": "tools/complete_quran_audio_pack.py changes this policy to required only after the pinned local pack passes full verification. Once required, normal builds fail closed if that exact local pack is missing or replaced."
}
