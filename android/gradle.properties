org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=1G -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Keep the existing API-24 pharmacy app installable. Local inference uses CPU;
# optional accelerators must not raise the whole app's minimum SDK to 28.
lib_llama_cpp.enable_nnapi=OFF
lib_llama_cpp.enable_vulkan=OFF
