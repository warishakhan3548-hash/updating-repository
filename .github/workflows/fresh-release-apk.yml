name: Fresh release APK

on:
  push:
    branches: [ main ]
    paths:
      - '.github/workflows/fresh-release-apk.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: gradle

      - name: Use temporary signing for installable release
        run: |
          python3 - <<'PY'
          from pathlib import Path
          p = Path("app/build.gradle")
          s = p.read_text()
          old = "buildTypes { release { minifyEnabled false; debuggable false } }"
          new = "buildTypes { release { minifyEnabled false; debuggable false; signingConfig signingConfigs.debug } }"
          if old not in s:
              raise SystemExit("Expected release buildTypes block not found")
          p.write_text(s.replace(old, new, 1))
          PY

      - name: Build release APK
        run: |
          chmod +x gradlew
          ./gradlew --no-daemon assembleRelease

      - name: Verify APK
        run: |
          APK="app/build/outputs/apk/release/app-release.apk"
          APKSIGNER="$(find "$ANDROID_SDK_ROOT/build-tools" -type f -name apksigner | sort -V | tail -1)"
          "$APKSIGNER" verify --verbose --print-certs "$APK"

      - name: Collect APK
        run: |
          mkdir -p release-apk
          cp app/build/outputs/apk/release/app-release.apk release-apk/Aaris-New-Release.apk
          sha256sum release-apk/Aaris-New-Release.apk
          ls -lh release-apk/

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: aaris-new-release-apk
          path: release-apk/Aaris-New-Release.apk
          if-no-files-found: error
          retention-days: 7
