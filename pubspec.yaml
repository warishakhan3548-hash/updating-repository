name: voice_ludo_masti
description: Aarish Kingdom — a polished local multiplayer Ludo game with instant turn-safe voice-controlled dice.
publish_to: "none"
version: 1.5.0+16

environment:
  sdk: ">=3.8.0 <4.0.0"
  flutter: ">=3.32.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
