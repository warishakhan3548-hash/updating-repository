# Quran search evaluation CI validation

This temporary PR marker exists only to trigger pull-request CI against the integrated `main` tree. It changes no runtime behavior, source data, content pack, schema, or trust boundary.
